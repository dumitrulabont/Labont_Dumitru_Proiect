@page
@model Labont_Dumitru_Proiect.Pages.Note.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nota[0].ValoareNota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nota[0].Student.Nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nota[0].Curs.Denumire) Curs
            </th>
            <th>
                 Profesor
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Nota) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ValoareNota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.Nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Curs.Denumire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Curs.Profesor.Nume)
                @Html.DisplayFor(modelItem => item.Curs.Profesor.Prenume)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
